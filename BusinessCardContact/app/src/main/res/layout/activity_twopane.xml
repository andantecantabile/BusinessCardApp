<?xml version="1.0" encoding="utf-8"?>
<!-- placeholder layout for a dual-pane landscape mode -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:divider="?android:attr/dividerHorizontal"
    android:showDividers="middle"
    android:orientation="horizontal">
    <!-- placeholder for left pane: the recycler view goes here -->
    <FrameLayout android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="?linearlayout_bg_2"
        />
    <!-- placeholder for right pane: the contact detail view goes here -->
    <FrameLayout
        android:id="@+id/detail_fragment_container"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:background="?linearlayout_bg_main"
        />
</LinearLayout>